import 'package:flutter/material.dart';

class ListViewDefault extends StatelessWidget {
  const ListViewDefault({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
